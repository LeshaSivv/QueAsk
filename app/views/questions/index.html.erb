<% provide :page_title, "All Que" %>
<% currently_at t('menu.questions') %>

<h1 class="mb-4 text-light"><%= t('.title') %></h1>

<%= link_to t('.new'), new_question_path, class: 'mb-3 btn btn-info btn-lg' %>

<%== pagy_bootstrap_nav(@pagy) if @pagy.pages > 1%>

<%= render partial: 'questions/question', collection: @questions, as: :question %>

<%== pagy_bootstrap_nav(@pagy) if @pagy.pages > 1 %>